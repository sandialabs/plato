variables: 
  GPU_ARCHITECTURE: 70
  RUNNER_HOME: /home/gitlab-runner
  SPACK_DEPENDENCY_KEY: spack_dependency_cache
  PE_BUILD_CACHE_KEY: platoengine_build_cache
  SPACK_MIRROR_DIR: plato-ci-dependency-mirror
  REPORTS_ARTIFACTS_DIR: reports-dir
  KCOV_DIR: ${RUNNER_HOME}/kcov
  SIERRA_DIR: ${RUNNER_HOME}/sierra_binaries
  CUBIT_DIR: /opt/cubit/16.02
  OPTIMISM_DIR: ${RUNNER_HOME}/optimism